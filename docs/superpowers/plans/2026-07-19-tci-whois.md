# Прямой whois TCI — план реализации

> **Для агентов:** исполнять через superpowers:subagent-driven-development,
> задача за задачей. Спека — `docs/superpowers/specs/2026-07-19-tci-whois-design.md`.

**Цель:** whois для .RU/.РФ/.SU идёт напрямую на `whois.tcinet.ru:43` (~37 мс)
вместо A-Parser Net::Whois (секунды-минуты), с жёсткой маршрутизацией по зоне и
фолбэком на A-Parser.

**Стек:** Python 3.12, stdlib `socket` (без новых зависимостей), pytest.

## Глобальные ограничения

- `integrations/` = только транспорт, логика в `services/` (конвенция проекта).
- `None` = «не проверено», НИКОГДА не перезаписывает проверенное значение.
- Ошибки внешних вызовов не глотать молча — факт фолбэка обязан быть виден.
- Хард-гейты (денежный `confirmed_by_human`, редактурный `mark_edited`) не
  трогаются ни строкой.
- Тесты герметичны: `_no_live_network` рубит `socket.getaddrinfo` ловушкой-
  BaseException. Живой сети в тестах нет — TCI подменяется явно.
- Русский язык в докстрингах, комментариях и коммитах.
- Живой формат ответа TCI выверен пробой 2026-07-19, фикстура ниже — дословная.

---

## Задача 1: транспорт TCI + маршрутизатор + переключение воронки

**Файлы:**
- Создать: `backend/app/integrations/whois_tci.py`
- Создать: `backend/app/services/whois.py`
- Изменить: `backend/app/services/scoring.py` (строки ~436, ~889, `_clients()`)
- Создать: `backend/tests/test_whois_tci.py`
- Изменить: тесты с фейковыми словарями клиентов — добавить ключ `"tci"`
  (`test_transitions.py`, `test_funnel.py`, `test_recheck_acquirability.py`,
  `test_pipeline.py`, `test_rescore.py` — искать `"aparser":` в фейк-словарях)

**Интерфейсы:**
- Отдаёт: `TciWhoisClient.probe(domain) -> dict`, `TciWhoisClient.handles(domain) -> bool`,
  `services.whois.probe(domain, clients) -> dict`
- Контракт словаря: `{"available": bool|None, "created": datetime|None,
  "free_date": date|None, "whois_source": str}` — надмножество контракта
  `AParserClient.whois_probe()` (`available`/`created`), потребители старых
  ключей не ломаются.

### Живая фикстура (дословный ответ, использовать в тестах)

```
% TCI Whois Service. Terms of use:
% https://tcinet.ru/documents/whois_ru_rf.pdf (in Russian)

domain:        USEFULSCRIPT.RU
nserver:       ns1.expired.reg.ru.
state:         REGISTERED, DELEGATED, VERIFIED
person:        Private Person
registrar:     REGRU-RU
created:       2011-06-17T08:12:35Z
paid-till:     2026-06-17T09:12:35Z
free-date:     2026-07-21
source:        TCI
```

Свободный домен: `No entries found for the selected source(s).`
Кириллица/мусор: `Invalid request.`

- [ ] **Шаг 1: тесты парсера (падают)** — `backend/tests/test_whois_tci.py`

```python
from datetime import date

from app.integrations.whois_tci import TciWhoisClient, _parse

_TAKEN = """% TCI Whois Service. Terms of use:

domain:        USEFULSCRIPT.RU
state:         REGISTERED, DELEGATED, VERIFIED
registrar:     REGRU-RU
created:       2011-06-17T08:12:35Z
paid-till:     2026-06-17T09:12:35Z
free-date:     2026-07-21
source:        TCI
"""
_FREE = ("% TCI Whois Service. Terms of use:\n\n"
         "No entries found for the selected source(s).\n")
_INVALID = "% TCI Whois Service. Terms of use:\n\nInvalid request.\n"


def test_parse_taken_domain():
    r = _parse(_TAKEN)
    assert r["available"] is False
    assert r["created"].year == 2011
    assert r["free_date"] == date(2026, 7, 21)


def test_parse_free_domain():
    r = _parse(_FREE)
    assert r["available"] is True
    assert r["created"] is None and r["free_date"] is None


def test_parse_invalid_request_is_unknown():
    """Invalid request — НЕ свободен и НЕ занят. Кириллический .рф без punycode
    отвечает именно так; трактовать его как свободный = утащить домен в выкуп."""
    assert _parse(_INVALID)["available"] is None


def test_parse_junk_is_unknown():
    assert _parse("")["available"] is None
    assert _parse("совершенно посторонний текст")["available"] is None


def test_handles_only_tci_zones():
    """example.com у TCI отвечает 'No entries found' — дословно как свободный .ru.
    Маршрутизация по зоне ОБЯЗАНА резать это до запроса (живая проба 2026-07-19)."""
    c = TciWhoisClient()
    assert c.handles("clara-c.ru") is True
    assert c.handles("ok.su") is True
    assert c.handles("xn--80aswg.xn--p1ai") is True
    assert c.handles("сайт.рф") is True          # нормализуется в punycode
    assert c.handles("example.com") is False
    assert c.handles("example.net") is False
```

- [ ] **Шаг 2: убедиться, что падают**

`.venv/bin/python -m pytest backend/tests/test_whois_tci.py -q` → ошибка импорта.

- [ ] **Шаг 3: транспорт** — `backend/app/integrations/whois_tci.py`

```python
"""Прямой whois координатора зон .RU/.РФ/.SU (whois.tcinet.ru:43). Транспорт only.

Формат ответа выверен ЖИВОЙ пробой 2026-07-19 (не по документации — дисциплина
проекта после двух дорогих ошибок разбора вслепую). См.
docs/superpowers/specs/2026-07-19-tci-whois-design.md.

Зачем мимо A-Parser: ~37 мс против секунд-минут и без расхода квоты. Зачем
ЖЁСТКО по зоне: на не-TCI домен (example.com) сервер отвечает 'No entries found'
дословно как на свободный .ru — наивная маршрутизация пометила бы каждый
занятый .com свободным.
"""
import re
import socket
from datetime import date, datetime, timezone

_HOST = "whois.tcinet.ru"
_PORT = 43
_TIMEOUT = 10.0

# Зоны, которые обслуживает этот сервер (punycode-хвосты). Всё остальное — A-Parser.
_ZONES = ("ru", "su", "xn--p1ai")

_CREATED_RE = re.compile(r"^created:\s*(\S+)", re.M)
_FREE_DATE_RE = re.compile(r"^free-date:\s*(\d{4}-\d{2}-\d{2})", re.M)
_DOMAIN_RE = re.compile(r"^domain:\s*\S+", re.M)


def _punycode(domain: str) -> str:
    """Кириллический .рф -> punycode. Сервер на кириллицу отвечает 'Invalid request.'"""
    d = domain.strip().lower().rstrip(".")
    try:
        return d.encode("idna").decode("ascii")
    except (UnicodeError, ValueError):
        return d


def _parse(text: str) -> dict:
    """Ответ whois -> {"available", "created", "free_date"}. Чистая функция, не бросает.

    available: False — есть запись `domain:`; True — 'No entries found';
    None — 'Invalid request.'/мусор (НЕ определить, не путать со свободным)."""
    created = free_date = None
    if _DOMAIN_RE.search(text):
        available = False
        m = _CREATED_RE.search(text)
        if m:
            try:
                created = datetime.fromisoformat(m.group(1).replace("Z", "+00:00"))
                if created.tzinfo is None:
                    created = created.replace(tzinfo=timezone.utc)
            except ValueError:
                created = None
        m = _FREE_DATE_RE.search(text)
        if m:
            try:
                free_date = date.fromisoformat(m.group(1))
            except ValueError:
                free_date = None
    elif "No entries found" in text:
        available = True
    else:
        available = None
    return {"available": available, "created": created, "free_date": free_date}


class TciWhoisClient:
    """Сырой TCP на порт 43. httpx здесь не при чём — whois не HTTP."""

    def handles(self, domain: str) -> bool:
        d = _punycode(domain)
        return any(d.endswith("." + z) for z in _ZONES)

    def query(self, domain: str) -> str:
        """Сырой ответ сервера. Сетевой сбой ПРОБРАСЫВАЕТСЯ — судит вызывающий
        (тот же контракт, что AParserClient.whois_probe)."""
        d = _punycode(domain)
        with socket.create_connection((_HOST, _PORT), timeout=_TIMEOUT) as s:
            s.sendall((d + "\r\n").encode("ascii"))
            chunks = []
            while True:
                c = s.recv(4096)
                if not c:
                    break
                chunks.append(c)
        return b"".join(chunks).decode("utf-8", "replace")

    def probe(self, domain: str) -> dict:
        return _parse(self.query(domain))

    def ping(self) -> bool:
        """Живость для /diag — запрос по заведомо занятому домену координатора."""
        return _parse(self.query("nic.ru"))["available"] is False
```

- [ ] **Шаг 4: тесты парсера зелёные**

`.venv/bin/python -m pytest backend/tests/test_whois_tci.py -q` → PASS.

- [ ] **Шаг 5: тесты маршрутизатора (падают)** — дописать в тот же файл

```python
class _FakeTci:
    def __init__(self, result=None, boom=False):
        self.result, self.boom, self.calls = result, boom, []

    def handles(self, domain):
        return TciWhoisClient().handles(domain)

    def probe(self, domain):
        self.calls.append(domain)
        if self.boom:
            raise OSError("соединение отклонено")
        return self.result


class _FakeAparser:
    def __init__(self):
        self.calls = []

    def whois_probe(self, domain):
        self.calls.append(domain)
        return {"available": False, "created": None}


def test_router_uses_tci_for_ru():
    from app.services import whois
    tci = _FakeTci({"available": True, "created": None, "free_date": None})
    ap = _FakeAparser()
    r = whois.probe("clara-c.ru", {"tci": tci, "aparser": ap})
    assert r["available"] is True
    assert r["whois_source"] == "tci"
    assert tci.calls == ["clara-c.ru"] and ap.calls == []


def test_router_uses_aparser_for_com():
    """Регресс на ловушку: .com НЕ должен уходить в TCI ни при каких условиях."""
    from app.services import whois
    tci = _FakeTci({"available": True, "created": None, "free_date": None})
    ap = _FakeAparser()
    r = whois.probe("example.com", {"tci": tci, "aparser": ap})
    assert r["whois_source"] == "aparser"
    assert tci.calls == [] and ap.calls == ["example.com"]


def test_router_falls_back_visibly_when_tci_fails():
    """Сбой TCI не глотается молча — фолбэк помечен в whois_source."""
    from app.services import whois
    tci, ap = _FakeTci(boom=True), _FakeAparser()
    r = whois.probe("clara-c.ru", {"tci": tci, "aparser": ap})
    assert r["whois_source"] == "aparser_fallback"
    assert ap.calls == ["clara-c.ru"]
    assert r["free_date"] is None
```

- [ ] **Шаг 6: маршрутизатор** — `backend/app/services/whois.py`

```python
"""Маршрутизация whois: зоны TCI (.ru/.рф/.su) — прямым сокетом, остальные —
через A-Parser. Логика выбора канала живёт здесь, а не в транспорте (конвенция
проекта: integrations/ = только транспорт).

См. docs/superpowers/specs/2026-07-19-tci-whois-design.md.
"""


def probe(domain: str, clients: dict) -> dict:
    """{"available", "created", "free_date", "whois_source"}.

    Надмножество контракта AParserClient.whois_probe() — старые потребители
    ключей available/created не ломаются. whois_source показывает, ЧЕМ судили
    домен: сбой TCI молча не превращается в «A-Parser так решил»."""
    tci = clients.get("tci")
    if tci is not None and tci.handles(domain):
        try:
            return {**tci.probe(domain), "whois_source": "tci"}
        except Exception:                      # noqa: BLE001 — сбой канала, не приговор домену
            source = "aparser_fallback"        # видно оператору в score_breakdown
        pr = clients["aparser"].whois_probe(domain)
    else:
        source = "aparser"
        pr = clients["aparser"].whois_probe(domain)
    return {"available": pr.get("available"), "created": pr.get("created"),
            "free_date": None, "whois_source": source}
```

- [ ] **Шаг 7: тесты маршрутизатора зелёные**

`.venv/bin/python -m pytest backend/tests/test_whois_tci.py -q` → PASS.

- [ ] **Шаг 8: переключить воронку** — `backend/app/services/scoring.py`

В `_clients()` добавить ключ рядом с существующими:

```python
    from app.integrations.whois_tci import TciWhoisClient
    ...
    "tci": TciWhoisClient(),
```

Строка ~436 (T1 в `_funnel`) — заменить прямой вызов:

```python
        pr = c["aparser"].whois_probe(d.domain)
```

на маршрутизатор (импорт — вверху модуля, не в теле функции):

```python
        pr = whois_router.probe(d.domain, c)
```

Сразу после успешного вызова зафиксировать канал в сигналах (рядом с
существующей записью `sig["acquirability_checked_at"]`):

```python
    sig["whois_source"] = pr.get("whois_source")
```

Строка ~889 (`recheck_acquirability`) — та же замена:

```python
                pr = whois_router.probe(name, c)
```

Импорт вверху `scoring.py`:

```python
from app.services import whois as whois_router
```

НИЧЕГО другого в логике воронки не менять: пороги, порядок ступеней,
`acquirability_verdict`, `reject_reason` остаются как есть. Задача —
подмена канала, не смена решений.

- [ ] **Шаг 9: починить фейковые словари клиентов в тестах**

Найти: `grep -rn '"aparser":' backend/tests`. В КАЖДОМ фейковом словаре
клиентов добавить ключ, который не уводит тест в живую сеть:

```python
        "tci": type("T", (), {"handles": lambda self, d: False})(),
```

`handles -> False` значит «этот фейк-тест судит A-Parser'ом», поведение
существующих тестов не меняется — они и писались против A-Parser.

- [ ] **Шаг 10: весь сьют + линт**

```bash
.venv/bin/python -m pytest backend/tests/ -q
.venv/bin/python -m pyflakes backend/app backend/tests
```

Ожидание: все тесты зелёные (было 702 + новые), pyflakes чист.

- [ ] **Шаг 11: коммит**

```bash
git add -A && git commit -F - <<'EOF'
feat(whois): прямой whois TCI для .RU/.РФ/.SU вместо A-Parser Net::Whois

T1 воронки платят ~77% доменов пула; A-Parser отвечает секундами-минутами и
жжёт квоту. whois.tcinet.ru:43 — ~37 мс, без ключа, stdlib socket.

Маршрутизация ЖЁСТКО по зоне: на example.com сервер отвечает 'No entries found'
дословно как на свободный .ru — наивный роутинг пометил бы занятые не-TCI
домены свободными. Сбой TCI -> фолбэк на A-Parser, факт виден в whois_source.

Формат ответа выверен живой пробой 2026-07-19, не по документации.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
```

---

## Задача 2: `free-date` как дата дропа

**Файлы:**
- Изменить: `backend/app/services/scoring.py` (T1, после успешного whois)
- Изменить: `backend/tests/test_whois_tci.py` (дописать)

**Интерфейсы:**
- Потребляет: `whois_router.probe()["free_date"]` из Задачи 1.

**Зачем отдельной задачей:** это смена ПОВЕДЕНИЯ воронки
(`acquirability_verdict` судит по `acquire_deadline`), а не ускорение. Закрывает
дыру «бездедлайновый пул» из CLAUDE.md: ~5.4 тыс. доменов без даты дропа
переопрашиваются вечно и не имеют терминального состояния.

- [ ] **Шаг 1: тест (падает)** — дописать в `backend/tests/test_whois_tci.py`

```python
def test_free_date_fills_missing_deadline_only(monkeypatch):
    """free-date заполняет ПУСТОЙ acquire_deadline. Уже известный дедлайн (из
    фида backorder — источник с ценой и лейном) НЕ перезаписывается."""
    from datetime import date, datetime, timezone
    from app.services import scoring

    known = datetime(2026, 8, 1, tzinfo=timezone.utc)
    for existing, expect_day in ((None, 21), (known, 1)):
        sig = {}
        pr = {"available": False, "created": None,
              "free_date": date(2026, 7, 21), "whois_source": "tci"}
        got = scoring._deadline_from_whois(existing, pr)
        assert got.day == expect_day
```

- [ ] **Шаг 2: убедиться, что падает**

`.venv/bin/python -m pytest backend/tests/test_whois_tci.py -q` → AttributeError.

- [ ] **Шаг 3: реализация** — `backend/app/services/scoring.py`

```python
def _deadline_from_whois(existing, pr: dict):
    """Дедлайн выкупа из whois free-date, ТОЛЬКО если его ещё нет.

    None = «не знаем», а известный дедлайн из фида backorder приходит вместе с
    лейном и ценой — он авторитетнее и перезаписи не подлежит (инвариант
    проекта: None никогда не затирает проверенное значение)."""
    from datetime import datetime, time, timezone
    if existing is not None:
        return existing
    fd = pr.get("free_date")
    if fd is None:
        return None
    return datetime.combine(fd, time.min, tzinfo=timezone.utc)
```

Вызвать в `_funnel` сразу после успешного whois, до вердикта приобретаемости:

```python
    d.acquire_deadline = _deadline_from_whois(d.acquire_deadline, pr)
```

- [ ] **Шаг 4: тест зелёный + весь сьют**

```bash
.venv/bin/python -m pytest backend/tests/ -q
.venv/bin/python -m pyflakes backend/app backend/tests
```

- [ ] **Шаг 5: коммит**

```bash
git add -A && git commit -F - <<'EOF'
feat(whois): free-date из TCI заполняет пустой acquire_deadline

Закрывает «бездедлайновый пул» (CLAUDE.md): домен без даты дропа
переопрашивался whois'ом вечно и не имел терминального состояния. Известный
дедлайн из фида backorder не перезаписывается — он приходит с лейном и ценой.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
```

---

## После обеих задач

Ревью всей ветки через `combine-reviewer`: два хард-гейта не тронуты, порядок
ступеней воронки и пороги не изменены, `None` нигде не затирает проверенное,
герметичность тестов цела.

Живая проверка на боксе (не в этой сессии): прогнать `score`, сверить, что
скорость упала с 28 с/домен и `score_breakdown.whois_source == "tci"` у
.ru-доменов.
