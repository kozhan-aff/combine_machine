fastapi>=0.115
uvicorn[standard]>=0.32
watchfiles>=0.24         # авто-reload воркера (см. worker command) — тянется uvicorn[standard], фиксируем явно
sqlalchemy>=2.0
psycopg[binary]>=3.2
alembic>=1.13
pydantic>=2.9
pydantic-settings>=2.5
httpx>=0.27
dnspython>=2.6           # Spamhaus/SURBL через свой DNS_RESOLVER (публичные резолверы блокируются)
tenacity>=9.0
apscheduler>=3.10
google-api-python-client>=2.140
google-auth>=2.34
jinja2>=3.1
nh3>=0.2                   # HTML sanitizer (published pages are public — strip hostile markup)
python-multipart>=0.0.9   # FastAPI form parsing (panel POST action buttons)
pytest>=8            # offline test harness (tests/) — runs on SQLite, no PG/Docker needed
